{
  "name": "flowdesk-os",
  "version": "1.0.1",
  "description": "FlowDesk Teacher Operating System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx prisma generate && npx prisma db push --accept-data-loss"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@quixo3/prisma-session-store": "^3.1.13",
    "bcrypt": "^5.1.1",
    "dompurify": "^3.0.6",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "jsdom": "^22.1.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
