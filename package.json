{
  "name": "teacher-digital-planner",
  "version": "1.0.0",
  "description": "Teacher Digital Planner",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx prisma generate && npx prisma db push --accept-data-loss"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "express": "^4.18.2",
    "openai": "^4.28.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
