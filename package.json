{
  "name": "teacher-digital-planner",
  "version": "1.0.0",
  "description": "Web-based digital planner and calendar sync",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx prisma generate && npx prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "prisma": "^5.10.2"
  }
}
